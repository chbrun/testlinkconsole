language: python
python:
    - "2.7"
install:
    - pip install -r requirements.txt
script:
    - python -m unittest discover
    - coverage run -m unittest discover testlinkconsole
after_success:
    - coveralls
